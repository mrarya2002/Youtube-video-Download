<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>YtSave</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous">
</head>
</head>
<body>
    <!-- navbar -->
    <nav class="navbar navbar-expand-lg bg-body-tertiary">
        <div class="container">
          <a class="navbar-brand fs-4 fw-bold text-danger" href="#">YtDownload</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav ms-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="#">Youtube Downloader</a>
              </li>
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="#">Youtube to MP3</a>
              </li>
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="#">Facebook Downloader</a>
              </li>
            </ul>
          </div>
        </div>
      </nav>

      <!--main content  -->
    <div class="container-fluid bg-dark text-light">
        <div class="container p-5 text-center">
            <h2 class="text-danger">Download Youtube video online</h2>
            <p>Download Youtube video 1080p, 4k, 8k on iPhone Android</p>   
            <form action="/download" method="get">
                <div class="d-flex flex-column flex-sm-row gap-2 m-auto" style="max-width: 450px;">
                    <input id="link" type="text" name="link" class="form-control" placeholder="Paste Youtube link here">
                    <input type="submit" class="btn btn-danger" value="Download">
                  </div>
            </form> 
        </div>
    </div>  
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ENjdO4Dr2bkBIFxQpeoTz1HIcje39Wm4jDKdf19U8gI4ddQ3GYNS7NTKfAdVQSZe" crossorigin="anonymous"></script>
</body>
</html>

<!-- 
    const ytdl = require('ytdl-core');
const fs = require('fs');

const videoUrl = 'https://www.youtube.com/watch?v=dQw4w9WgXcQ';
const outputFile = 'video.mp4';

const video = ytdl(videoUrl, { filter: format => format.container === 'mp4' });

video.pipe(fs.createWriteStream(outputFile));

video.on('progress', (chunkLength, downloaded, total) => {
  console.log(`Downloaded ${downloaded} bytes of ${total}`);
});

video.on('end', () => {
  console.log('Finished downloading video');
});

 -->



 <!-- second way -->
 <!-- const ytdl = require('ytdl-core');
const prompt = require('prompt-sync')();
const fs = require('fs');

const downloadVideo = () => {
  const videoUrl = prompt('Enter the YouTube video URL: ');
  const downloadLocation = prompt('Enter the download location: ');

  ytdl(videoUrl)
    .pipe(fs.createWriteStream(downloadLocation));
};

downloadVideo();
 -->